package cr.co.ctpcit.citsacbackend.data.enums;

/**
 * This ENUM match with the DataSource WeekDays enum in tbl_examdays,
 * and it is used to determine the days of the week that the exam is going to be applied.
 */
public enum WeekDays {
    M, K, W, T, F, S, SS
}
