package cr.co.ctpcit.citsacbackend.data.enums;

/**
 * This ENUM match with the DataSource DocType enum in tbl_documents,
 * and it is used to determine the type of document that the student has, in this way:
 * HC: Health Certificate
 * OT: Other
 */
public enum DocType {
    HC, OT
}
